{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open in Browser",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}/client",
      "preLaunchTask": "Start Blog (前后端一起启动)"
    }
  ]
}
